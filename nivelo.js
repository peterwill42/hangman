var selx=10;

function niveloj()
{
  selx = parseInt(document.getElementById("selekto").value);
  document.getElementById("provado").innerHTML = selx;


//store and retrieve selx (preserve setting)
//window.sessionStorage.setItem("konservu", selx);
//var konservu = parseInt(window.sessionStorage.getItem("konservu"));
};
